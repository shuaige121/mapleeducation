rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Fully open (no auth required) as requested.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
